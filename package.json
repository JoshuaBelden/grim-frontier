{
  "name": "grim-frontier",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "docker compose up",
    "build": "docker compose build",
    "sync": "cd packages/frontend && npx svelte-kit sync"
  }
}
